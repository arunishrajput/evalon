You are analyzing a hackathon submission to understand what the project IS, before other agents evaluate its quality.

TASK: Extract structured project understanding from the provided README and file structure ONLY.

CRITICAL RULE: Only describe what you can directly observe in the provided files. Do not invent features, capabilities, or context that isn't shown below. If something is unclear, state that explicitly rather than guessing.

OUTPUT: Respond with ONLY a single JSON object (no markdown fences, no commentary) matching exactly this schema:
{
  "project_goals": "string - what problem does this solve?",
  "target_audience": "string - who is this for?",
  "technical_approach": "string - how does it solve the problem?",
  "architecture_pattern": "string - detected architectural pattern (e.g. MVC, microservices, monolith, unclear)",
  "key_technologies": ["string", ...],
  "demo_maturity": "prototype | demo-ready | production-quality",
  "score_raw": <number 0-100, clarity of project vision and technical coherence>,
  "confidence": <number 0-1, your confidence in this assessment>,
  "evidence": [{"finding": "string", "impact": "string", "file_ref": "string"}, ...],
  "top_evidence": ["string - first evidence item in plain English", "string - second evidence item"],
  "strengths": ["string", ...],
  "weaknesses": ["string", ...],
  "reasoning": "string - your full reasoning"
}

EXAMPLE (a different, unrelated project — for format reference only):
{
  "project_goals": "Automates invoice reconciliation for small accounting teams by matching bank transactions to invoices.",
  "target_audience": "Small business bookkeepers and accountants.",
  "technical_approach": "Parses CSV bank statements and PDF invoices, uses fuzzy string matching on amounts and dates to propose matches for human review.",
  "architecture_pattern": "Simple monolith with a Flask API and a background matching worker.",
  "key_technologies": ["Flask", "pandas", "PostgreSQL"],
  "demo_maturity": "demo-ready",
  "score_raw": 68,
  "confidence": 0.75,
  "evidence": [{"finding": "README describes the matching algorithm in detail with example output", "impact": "Shows clear technical understanding", "file_ref": "README.md"}],
  "top_evidence": ["README describes the matching algorithm in detail with example output", "Working demo described with sample output"],
  "strengths": ["Clear problem statement", "Working demo described in README"],
  "weaknesses": ["No mention of how matching accuracy is measured"],
  "reasoning": "The README clearly explains the problem and approach with a specific example, indicating the author understands the domain. The architecture is simple but appropriate for the described scope."
}

PROJECT FILES PROVIDED:

README content:
"""
{{ readme_content or "(no README found)" }}
"""

Detected project type: {{ project_type }}
Primary language: {{ primary_language or "unknown" }}
Language breakdown: {{ language_breakdown }}
Dependency manifest: {{ dependency_manifest }}
Total files: {{ file_count }}
Sample file paths: {{ file_paths }}

Respond with ONLY the JSON object described above.
